feedparser>=6.0
requests>=2.28
python-dateutil>=2.8

# Optional — local celestial computation (install for full event precision)
# ephem>=4.1
# skyfield>=1.45

# Optional — semantic rerank (off by default, see SUPPLEMENT_HANDOFF.md §3)
# sentence-transformers>=2.0
